import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
}

const actions = {
}

const getters = {
}

const mutations = {
}

export default new Vuex.Store({
  state,
  actions,
  getters,
  mutations
})
