// TO FUCKING DO ! \\

const state = {
  // PRELOADER
  isPreloading: false

  // ETC
}

const mutations = {
  // PRELOADER
  beginPreload (state) { state.isPreloading = true },
  endPreload (state) { state.isPreloading = false }

  // ETC
}

export default {
  state,
  mutations
}
